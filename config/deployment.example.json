{
  "deployment_name": "local-dev",
  "enabled_lines": [
    "marketing"
  ],
  "line_manifest_files": {
    "marketing": "line-packs/marketing/manifest.json"
  },
  "notes": [
    "The default deployment home is ~/.gency unless GENCY_HOME overrides it.",
    "Relative line_manifest_files paths resolve from the deployment manifest directory.",
    "Use environment variables or runtime arguments if you want to override manifest_root, prompt_root, state_root, lines_root, or registry_root."
  ]
}
